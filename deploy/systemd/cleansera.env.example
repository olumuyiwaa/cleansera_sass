# Example environment file for systemd service
# Copy to /etc/default/cleansera and adjust values
NODE_ENV=production
DATABASE_URL=postgresql://user:pass@db-host:5432/cleansera
REDIS_URL=redis://:password@redis-host:6379/0
# Optional: path to node binary if not in default PATH
#NODE_BIN=/usr/bin/node
